# commands_restart

## 概要
（プロジェクトの説明をここに記載）

## セットアップ
1. 仮想環境を有効化: `.venv\Scripts\Activate.ps1`
2. パッケージインストール: `pip install -r requirements.txt`

## 使い方
（使い方をここに記載）

## ログ
実行ログは `logs/` フォルダに出力されます。
ファイル名形式: `app_YYYYMMDD_HHMMSS.log`
