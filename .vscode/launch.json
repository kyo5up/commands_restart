{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: main.py (venv)",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "python": "${workspaceFolder}/.venv/Scripts/python.exe",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
